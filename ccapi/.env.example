# 服务端口
PORT=3000

# 对外访问地址（可选）
# 留空：自动根据请求的 Host 头动态拼接图片 URL（推荐，适配多环境）
# 配置：强制使用该值（例如接入 CDN 或自定义域名时）
# BASE_URL=https://api.example.com

# SQLite 数据库文件路径
DB_PATH=./data/meal.db

# 图片上传目录（相对路径或绝对路径）
UPLOAD_DIR=./uploads

# 单张图片大小限制（字节），默认 10MB
MAX_FILE_SIZE=10485760

# 单次最多上传图片数量
MAX_FILES=9
