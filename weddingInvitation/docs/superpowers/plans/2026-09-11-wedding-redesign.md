# 婚礼影集改版

目标：移除原邀约页，整合必要文案，增加精选实拍婚纱照并优化加载，保留原有北京时间倒计时和地图导航。

视觉：从现有封面的蓝天与白纱取色，以深蓝、象牙白、细线和衬线文字统一四个章节；婚纱照为主要视觉，避免重复封面和大块信息卡片。

- [x] 素材：从精修目录选择封面与六张补充照片；原图保留，生成两档 WebP 和 JPEG 回退，记录文件大小与来源。
- [x] 结构：App 仅保留封面、倒计时、婚礼信息、收尾四章；移除 InvitationSection 的挂载和旧锚点。共享图片与页脚组件负责响应式图片和一致的翻页导航。
- [x] 页面：封面强化标题与新人姓名；倒计时结合双照片、保留原计时逻辑；信息页显示完整地址与高德入口；收尾将三张照片和原邀约文案结合。
- [x] 验证：只运行 countdown 和 navigation 的定向测试、修改的 Vue/SCSS 编译检查及移动端页面检查。核对四章导航、图片加载、窄屏/横屏和减少动态效果。不执行全项目构建、测试或部署。
- [x] 文档：更新 README 中四章结构、图片维护方法和实际压缩结果。

约束：不新增依赖、不改变姓名/时间/地点、不修改原始照片或已有分享图，不提交、不改 dist，不撤销工作区已有修改。

验证记录：countdown/navigation 7 条定向用例通过；7 个修改的 Vue 组件与全局 SCSS 编译通过；照片配置的定向 TypeScript 检查通过；git diff --check 通过。浏览器核对 320/360/375/390/412/430px 及 844×390 横屏无横向溢出；360/375/390px 常见手机高度下四章均为一屏；四个锚点正确，7 张图片均加载 WebP，倒计时数值匹配北京时间，检查期间没有新的 console error/warn。未运行全量构建或测试、未部署。
